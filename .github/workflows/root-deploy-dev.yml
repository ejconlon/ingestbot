# Workflow for deployments to the dev envrionment
name: root-deploy-dev
on:
  push:
    branches:
    - deploy-dev
jobs:
  integrate:
    uses: ./.github/workflows/sub-integrate.yml
  cdk-deploy:
    needs: [integrate]
    uses: ./.github/workflows/sub-cdk-deploy.yml
    with:
      env: dev
